[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# CMD comes from the Dockerfile.
restartPolicyType = "NEVER"

[[cron]]
schedule = "*/30 * * * *"  # every 30 minutes
command = "uv run excalibur ship"
